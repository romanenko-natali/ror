<h1>Task Details</h1>

<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<%= link_to "Edit", edit_project_task_path(@project, @task), class: "btn btn-warning" %> |
<%= link_to "Back", project_tasks_path(@project), class: "btn btn-secondary" %>
